# B系统使用指南

**文档版本**: v1.0
**创建日期**: 2026-01-13
**系统**: B方案（分形拓扑智能）

---

## 📋 快速开始

### 1. 启动B系统

**推荐：交互模式（适合第一次使用）**

```bash
python run_b_system.py --mode interactive
```

这将启动B系统并进入交互模式，你可以手动执行命令。

### 2. 观测B系统

**在另一个命令行窗口运行：**

```bash
python scripts/observe_system.py --quick-start
```

这将显示B系统的实时状态和监控数据。

---

## 🚀 运行模式

### 模式1：演示模式（自动执行）

```bash
python run_b_system.py --mode demo
```

**特点**:
- 每5秒自动执行一次决策
- 实时展示决策结果
- 每10次决策显示完整仪表板
- 适合观察系统自动运行

**输出示例**:
```
[决策 1] 响应=14.23ms, 置信度=0.5012, 熵=0.0000, 来源=fractal, 验证=需要
[决策 2] 响应=13.98ms, 置信度=0.4998, 熵=0.0000, 来源=fractal, 验证=需要
...
```

### 模式2：交互模式（手动控制）⭐ 推荐

```bash
python run_b_system.py --mode interactive
```

**特点**:
- 手动输入命令控制
- 可以执行单个决策或批量决策
- 查看详细系统状态
- 适合学习和调试

**可用命令**:
```
[B系统] > help          # 显示帮助信息
[B系统] > status        # 显示完整系统状态
[B系统] > decision      # 执行一次决策
[B系统] > batch         # 批量执行10次决策
[B系统] > clear         # 清屏
[B系统] > exit          # 退出系统
```

**使用示例**:
```python
# 启动系统
> python run_b_system.py --mode interactive

# 执行一次决策
[B系统] > decision

[决策结果]
  响应时间: 14.23ms
  置信度: 0.5012
  熵值: 0.0000
  来源: fractal
  需要验证: 是

# 查看系统状态
[B系统] > status

# 批量执行10次
[B系统] > batch

# 退出
[B系统] > exit
```

### 模式3：服务模式（后台运行）

```bash
python run_b_system.py --mode service
```

**特点**:
- 后台持续运行
- 每5秒自动执行决策
- 每60秒显示一次状态
- 适合长期运行

**输出示例**:
```
[07:35:23] 决策 #12 - 响应=14.12ms, 置信度=0.5003
[07:36:23] 决策 #24 - 响应=13.89ms, 置信度=0.5021
...
```

---

## 📊 观测系统

### 方法1：使用观测脚本（推荐）

```bash
python scripts/observe_system.py --quick-start
```

**输出内容**:
- B系统运行状态
- 启动信息和配置
- 监控仪表板
- 最近日志
- 快速启动指南

### 方法2：直接查看监控数据

**监控文件位置**:
```
monitoring/
├── b_system_startup.json      # 启动信息
├── metrics_20260113_073000.json # 监控数据（每10分钟）
└── b_system_final_stats.json   # 最终统计（退出时生成）
```

**查看启动信息**:
```bash
cat monitoring/b_system_startup.json
```

**查看监控数据**:
```bash
# Windows
type monitoring\metrics_*.json

# Linux/Mac
cat monitoring/metrics_*.json
```

### 方法3：使用Python查看

```python
import json
from pathlib import Path

# 读取启动信息
startup_file = Path("monitoring/b_system_startup.json")
with open(startup_file) as f:
    startup = json.load(f)
print(startup)

# 读取监控数据
metrics_files = list(Path("monitoring").glob("metrics_*.json"))
if metrics_files:
    latest = sorted(metrics_files)[-1]
    with open(latest) as f:
        metrics = json.load(f)
    print(f"总数据点: {len(metrics)}")
```

---

## 📈 系统状态说明

### 实时仪表板

运行 `status` 命令或使用观测脚本可以看到：

```
======================================================================
                       B系统实时状态仪表板
======================================================================

[运行信息]
  启动时间: 2026-01-13 07:30:23
  运行时长: 0:05:30
  总决策数: 66

[性能指标 - 最近5分钟]
  总请求数: 66
  平均响应时间: 14.05ms
  P50响应时间: 13.98ms
  P95响应时间: 16.23ms
  P99响应时间: 19.11ms

[智能指标]
  平均置信度: 0.5004
  平均熵值: 0.0000
  外部依赖率: 100.0%
  本地决策率: 0.0%

[决策来源]
  来源分布: {'fractal': 66}

======================================================================
```

### 指标解释

**响应时间指标**:
- `平均响应时间`: 所有决策的平均响应时间
- `P50/P95/P99`: 50%/95%/99%的决策响应时间低于此值
- 目标: <100ms（优秀），<200ms（良好）

**智能指标**:
- `平均置信度`: 决策的平均置信度（0-1）
  - 越高越好，说明系统越确定
  - B组应该显著高于A组
- `平均熵值`: 决策的随机性/探索程度（0-1）
  - 0 = 确定性决策
  - 1 = 完全随机探索
- `外部依赖率`: 需要外部验证的决策比例
  - 目标: <20%
  - 当前100%是正常的（网络未训练）
- `本地决策率`: 系统自主决策的比例
  - 目标: >80%
  - 会随着使用时间提升

---

## 🛑 停止系统

### 优雅停止

在B系统运行窗口按 `Ctrl+C`

系统会：
1. 捕获中断信号
2. 停止监控系统
3. 导出最终统计数据
4. 优雅退出

### 强制停止

如果优雅停止无效：

**Windows**:
```bash
# 查找Python进程
tasklist | findstr python

# 强制终止（使用PID）
taskkill /PID <进程ID> /F
```

**Linux/Mac**:
```bash
# 查找Python进程
ps aux | grep python

# 强制终止
kill -9 <进程ID>
```

---

## 🔧 常见问题

### Q1: B系统启动后没有反应？

**A**: 确保你选择了一个运行模式：
```bash
# 推荐：交互模式
python run_b_system.py --mode interactive
```

### Q2: 外部依赖率为什么是100%？

**A**: 这是正常的！原因：
1. 网络使用随机初始化参数（未训练）
2. 置信度阈值0.5，当前置信度刚好在边界
3. 随着真实数据的使用，置信度会提升，外部依赖会降低

**预期改善**:
- 1周后: 50-60%
- 2周后: 30-40%
- 1个月后: 10-20% ✅

### Q3: 如何查看历史监控数据？

**A**: 监控数据保存在 `monitoring/metrics_*.json`：
```bash
# 列出所有监控文件
dir monitoring\metrics_*.json  # Windows
ls monitoring/metrics_*.json   # Linux/Mac

# 查看最新数据
python scripts/observe_system.py
```

### Q4: 可以同时运行A系统和B系统吗？

**A**: 不建议。两个系统会竞争资源，且可能产生冲突。建议：
1. 停止A系统
2. 启动B系统
3. 观察B系统效果
4. 如果不满意，可以回滚到A系统

### Q5: 如何回滚到A系统？

**A**: A系统文件已备份：
```bash
# 备份位置
backups/system_switch_20260113_072742/

# 恢复A系统
python AGI_Life_Engine.py  # 直接运行原A系统
```

---

## 📁 文件结构

```
AGI/
├── run_b_system.py              # B系统主运行脚本 ⭐
│
├── scripts/
│   ├── observe_system.py        # 观测脚本 ⭐
│   ├── check_system.py          # 检查系统状态
│   ├── stop_system.py           # 停止系统
│   └── switch_to_production.py  # 系统切换脚本
│
├── monitoring/
│   ├── b_system_startup.json      # B系统启动信息
│   ├── metrics_*.json             # 监控数据文件
│   └── b_system_final_stats.json  # 最终统计
│
├── core/
│   ├── fractal_intelligence.py   # 分形智能核心
│   └── fractal_adapter.py        # 集成适配器
│
├── docs/
│   ├── A系统日志.txt             # A系统日志（只读）
│   ├── DEPLOYMENT_SUCCESS_REPORT_20260113.md
│   └── SYSTEM_STATUS_REPORT_20260113.md
│
└── backups/
    └── system_switch_20260113_072742/  # A系统备份
```

---

## 💡 使用建议

### 第一次使用

1. **以交互模式启动**，了解系统行为：
   ```bash
   python run_b_system.py --mode interactive
   ```

2. **在另一个窗口观测**：
   ```bash
   python scripts/observe_system.py --quick-start
   ```

3. **执行一些命令**，了解系统响应：
   ```
   > status
   > decision
   > batch
   ```

### 长期使用

1. **以服务模式运行**，让系统持续学习：
   ```bash
   python run_b_system.py --mode service
   ```

2. **定期观测指标**，关注改善趋势：
   ```bash
   python scripts/observe_system.py
   ```

3. **查看监控数据**，了解系统进展：
   - 外部依赖率是否下降
   - 置信度是否提升
   - 响应时间是否稳定

### 性能调优

如果需要调整系统参数，编辑配置：
```python
# run_b_system.py 或 config/production_config.py

confidence_threshold = 0.5  # 置信度阈值（0.4-0.7）
entropy_temperature = 2.0   # 熵温度参数（1.0-3.0）
```

---

## 📞 获取帮助

### 查看命令帮助

```bash
# B系统运行器帮助
python run_b_system.py --help

# 观测脚本帮助
python scripts/observe_system.py --help
```

### 在系统内查看帮助

启动交互模式后：
```
[B系统] > help
```

---

## ✅ 总结

**启动B系统**:
```bash
python run_b_system.py --mode interactive
```

**观测B系统**:
```bash
python scripts/observe_system.py --quick-start
```

**停止B系统**:
按 `Ctrl+C`

**三个关键文件**:
1. `run_b_system.py` - 启动B系统
2. `scripts/observe_system.py` - 观测系统
3. `monitoring/b_system_startup.json` - 启动信息

**推荐工作流**:
1. 启动B系统（交互模式）
2. 观测系统状态（另一个窗口）
3. 执行决策命令
4. 观察指标改善
5. 长期运行，让系统学习

---

**祝使用愉快！** 🎉

如有问题，查看监控数据或参考本文档。
