测试用例名称：验证 execute_visual_click 函数的精确点击能力

测试目标：验证 core/desktop_automation.py 文件中的 execute_visual_click 函数能够基于视觉识别技术在指定目标图像上执行精确的鼠标点击操作。

前置条件：
1. 系统为支持图形界面的桌面操作系统（Windows/macOS/Linux）。
2. Python 环境已安装并配置完成，包含依赖库如 OpenCV、PyAutoGUI、numpy 等。
3. core/desktop_automation.py 文件存在且 execute_visual_click 函数已实现。
4. 测试环境中存在一个稳定的图形用户界面应用（例如记事本、画图工具或自定义测试窗口），其中包含可识别的按钮或图标。
5. 已准备用于匹配的目标图像样本（template_image.png），该图像清晰、无失真，并与待点击区域完全匹配。

测试步骤：
1. 启动测试应用程序（例如 Windows 记事本 或 自定义测试窗口）。
2. 使用截图工具截取应用界面上某个可交互元素（如“文件”菜单、“新建”按钮）作为模板图像 template_image.png，并保存至指定资源目录。
3. 调用 execute_visual_click 函数，传入 template_image.png 的路径作为参数，设置置信度阈值为 0.95，启用高精度模式（如有相关参数）。
4. 执行函数后等待 1 秒，观察是否成功触发对应 UI 元素的功能（例如“文件”菜单展开）。
5. 捕获屏幕日志或通过辅助断言机制判断点击是否发生在目标区域中心点附近（允许 ±5 像素误差）。
6. 重复执行三次以排除偶然性干扰。

预期结果：
1. execute_visual_click 成功定位到目标图像所在区域。
2. 鼠标光标移动至匹配区域的中心位置并执行单击操作。
3. 对应 UI 元素响应点击事件（如菜单展开、窗口切换等）。
4. 连续三次执行均能准确触发功能，无误点击或未点击情况。
5. 日志输出显示匹配坐标、相似度得分及操作状态（成功/失败）。

异常处理：
- 若目标图像未找到，函数应抛出明确异常（如 ImageNotFoundException）并记录错误信息。
- 若屏幕缩放比例非 100%，需提前检测并进行坐标映射校正，确保点击位置准确。

测试结论判定：
当所有预期结果均满足时，判定 execute_visual_click 函数具备精确点击能力；否则，需检查图像匹配算法、坐标转换逻辑或环境适配问题，并进行修复与回归测试。